# OMO Slim v2.0.0 + Superpowers Patch-Kit v2.0.0 实施计划

> **For agentic workers:** REQUIRED SUB-SKILL: Use superpowers:subagent-driven-development (recommended) or superpowers:executing-plans to implement this plan task-by-task. Steps use checkbox (`- [ ]`) syntax for tracking.

**Goal:** 将 patch-kit 的访问控制层移植到 OMO Slim v2.0.0，新增 background-orchestration companion skill，使 superpowers 工作流在异步 background subagent 模型下正常运行。

**Architecture:** 两阶段实施 —— Phase A 将 15 个零依赖策略文件 + 5 处轻量编辑直接放入 v2.0.0 源码；Phase B 新增 1 个 companion skill + 2 个 bridge 扩展实现异步编排语义。不 fork superpowers，不重写现有技能。

**Tech Stack:** OMO Slim v2.0.0 (TypeScript), superpowers v5.1.0 (Markdown skills), OpenCode (native task tool), Bun

**前置条件（已确认）：**
- [x] 上下文继承：task packet 模式可替代 read_session
- [x] 完成回调：synthetic message injection 机制存在
- [x] Verification gate：双层验证架构可行
- [x] superpowers 技能零 subtask 引用，无需重写

**关键设计决策：**
- 移除补丁 0007 全部内容（preroute 在 background model 下无意义）
- 移除补丁 0004 的 shadow agent 注册部分（OpenCode 原生处理模型回退）
- 保留补丁 0004 的 orchestrator 前缀匹配（`isOrchestratorAgent()` 对 prompt bridge 注入仍然必需）
- 不 fork superpowers，新增技能作为独立 companion skill 分发

---

## Phase A：静态策略移植（目标：v2.0.0 编译通过 + 访问控制层就位）

### Task A1：准备工作 —— 获取 v2.0.0 源码

**Files:**
- 克隆目标: `oh-my-opencode-slim` (tag: `v2.0.0`)

- [ ] **Step 1: 克隆 OMO Slim v2.0.0**

```bash
git clone https://github.com/alvinunreal/oh-my-opencode-slim.git omo-slim-v2-target
Set-Location omo-slim-v2-target; git checkout v2.0.0
```

- [ ] **Step 2: 安装依赖，确认基准确认编译**

```bash
bun install
bun run build
```

预期: 构建成功，无错误。

- [ ] **Step 3: 运行现有测试套件确认基线**

```bash
bun test
```

记录基线测试结果（通过数/失败数），后续每次修改后复跑。

- [ ] **Step 4: 创建功能分支**

```bash
git checkout -b patch-kit-v2
```

### Task A2：移植 15 个独立策略文件

**说明：** 以下文件从 patch-kit 的 `oh-my-opencode-slim-patched/src/` 直接复制到 v2.0.0 源码对应目录。这些文件零依赖 `subtask`/`read_session`/`auto_continue`，无需任何修改。

**Files:**
- 复制源: `E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\`
- 复制目标: `omo-slim-v2-target\src\`

- [ ] **Step 1: 复制 superpowers policy 模块（补丁 0001）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\cli\superpowers-policy.ts" -Destination "omo-slim-v2-target\src\cli\superpowers-policy.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\cli\superpowers-policy.test.ts" -Destination "omo-slim-v2-target\src\cli\superpowers-policy.test.ts"
```

验证: `omo-slim-v2-target\src\cli\superpowers-policy.ts` 文件存在，包含 `AGENT_ALLOWED_SUPERPOWERS`、`resolveBaseAgentName()`、`isOrchestratorAgent()` 等导出。

- [ ] **Step 2: 复制权限分层模块（补丁 0006）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\cli\agent-tier-policy.ts" -Destination "omo-slim-v2-target\src\cli\agent-tier-policy.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\cli\agent-tier-policy.test.ts" -Destination "omo-slim-v2-target\src\cli\agent-tier-policy.test.ts"
```

- [ ] **Step 3: 复制限制 MCP 黑名单模块（补丁 0006）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\config\agent-mcp-blacklist.ts" -Destination "omo-slim-v2-target\src\config\agent-mcp-blacklist.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\config\agent-mcp-blacklist.test.ts" -Destination "omo-slim-v2-target\src\config\agent-mcp-blacklist.test.ts"
```

- [ ] **Step 4: 复制保留技能模块（补丁 0006）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\config\orchestrator-only-skills.ts" -Destination "omo-slim-v2-target\src\config\orchestrator-only-skills.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\config\orchestrator-only-skills.test.ts" -Destination "omo-slim-v2-target\src\config\orchestrator-only-skills.test.ts"
```

- [ ] **Step 5: 复制 orchestrator 身份模块（补丁 0004 的策略部分）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\utils\orchestrator-identity.ts" -Destination "omo-slim-v2-target\src\utils\orchestrator-identity.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\utils\orchestrator-identity.test.ts" -Destination "omo-slim-v2-target\src\utils\orchestrator-identity.test.ts"
```

- [ ] **Step 6: 复制配置合并工具（补丁 0006）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\utils\merge-agent-config.ts" -Destination "omo-slim-v2-target\src\utils\merge-agent-config.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\utils\merge-agent-config.test.ts" -Destination "omo-slim-v2-target\src\utils\merge-agent-config.test.ts"
```

- [ ] **Step 7: 复制冷却追踪模块（补丁 0005）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\hooks\foreground-fallback\cooldowns.ts" -Destination "omo-slim-v2-target\src\hooks\foreground-fallback\cooldowns.ts"
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\src\hooks\foreground-fallback\cooldowns.test.ts" -Destination "omo-slim-v2-target\src\hooks\foreground-fallback\cooldowns.test.ts"
```

- [ ] **Step 8: 复制构建清理脚本（补丁 0007 的构建部分）**

```powershell
Copy-Item -LiteralPath "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\oh-my-opencode-slim-patched\scripts\clean-dist.ts" -Destination "omo-slim-v2-target\scripts\clean-dist.ts"
```

- [ ] **Step 9: 验证文件准备就绪**

```powershell
# 应返回 17 个文件（16 个 .ts 源 + 1 个脚本）
Get-ChildItem -Recurse -LiteralPath "omo-slim-v2-target\src" -Include "superpowers-policy.ts","superpowers-policy.test.ts","agent-tier-policy.ts","agent-tier-policy.test.ts","agent-mcp-blacklist.ts","agent-mcp-blacklist.test.ts","orchestrator-only-skills.ts","orchestrator-only-skills.test.ts","orchestrator-identity.ts","orchestrator-identity.test.ts","merge-agent-config.ts","merge-agent-config.test.ts","cooldowns.ts","cooldowns.test.ts","clean-dist.ts" | Select-Object FullName
```

预期输出: 15 个文件完整列表。

- [ ] **Step 10: 首次编译测试**

```bash
bun run build
```

预期: 可能有未解析的 import 错误（因为 `skills.ts`、`agent-mcps.ts` 等文件尚未修改导入语句）。记录错误列表，后续 Task A3/A4 处理。

- [ ] **Step 11: 提交**

```bash
git add src/cli/superpowers-policy.ts src/cli/superpowers-policy.test.ts
git add src/cli/agent-tier-policy.ts src/cli/agent-tier-policy.test.ts
git add src/config/agent-mcp-blacklist.ts src/config/agent-mcp-blacklist.test.ts
git add src/config/orchestrator-only-skills.ts src/config/orchestrator-only-skills.test.ts
git add src/utils/orchestrator-identity.ts src/utils/orchestrator-identity.test.ts
git add src/utils/merge-agent-config.ts src/utils/merge-agent-config.test.ts
git add src/hooks/foreground-fallback/cooldowns.ts src/hooks/foreground-fallback/cooldowns.test.ts
git add scripts/clean-dist.ts
git commit -m "feat: transplant 15 independent patch-kit strategy files to v2.0.0"
```

### Task A3：修改 `src/cli/skills.ts` —— 接入 superpowers 策略

**Files:**
- Modify: `omo-slim-v2-target\src\cli\skills.ts`

**说明：** 在 `getSkillPermissionsForAgent()` 开头注入 superpowers 策略调用。参考补丁 0001 对 skills.ts 的改动。

- [ ] **Step 1: 打开文件，定位 `getSkillPermissionsForAgent` 函数**

确认 v2.0.0 中该函数签名和结构与 v1.1.1 一致。预期函数体开头为：

```typescript
export function getSkillPermissionsForAgent(
  agent: AgentConfig,
  skills: SkillConfig[],
): Record<string, 'allow' | 'deny'> {
```

- [ ] **Step 2: 在文件顶部添加导入**

```typescript
// 在现有 import 块末尾追加以下三行：
import { buildSuperpowersSkillPermissions } from './superpowers-policy';
import { getDefaultNonSpPolicy } from './agent-tier-policy';
import { applyReservedSkillOverrides } from '../config/orchestrator-only-skills';
```

- [ ] **Step 3: 在 `getSkillPermissionsForAgent` 函数体开头注入策略调用**

在 `const permissions: Record<string, 'allow' | 'deny'> = {};` 之后，添加：

```typescript
// --- patch-kit v2: superpowers policy injection ---
const spPermissions = buildSuperpowersSkillPermissions(agentName);
Object.assign(permissions, spPermissions);

// Non-SP skill default policy (tier-based)
const nonSpDefault = getDefaultNonSpPolicy(agentName);
permissions['*'] = nonSpDefault;
// --- end patch-kit v2 ---
```

- [ ] **Step 4: 在函数末尾（return 之前）添加保留技能覆盖**

```typescript
// --- patch-kit v2: reserved orchestrator-only skills override ---
applyReservedSkillOverrides(agentName, permissions);
// --- end patch-kit v2 ---
```

- [ ] **Step 5: 编译验证**

```bash
bun run build
```

预期: 如果 `skills.ts` 的导入解析正确，此步应通过。如果报 "module not found"，检查导入路径。

- [ ] **Step 6: 提交**

```bash
git add src/cli/skills.ts
git commit -m "feat: inject superpowers policy + tier policy + reserved skills into skills.ts"
```

### Task A4：修改 `src/cli/custom-skills.ts` —— simplify 技能归属调整

**Files:**
- Modify: `omo-slim-v2-target\src\cli\custom-skills.ts`

- [ ] **Step 1: 找到 `simplify` 技能定义**

搜索 `name: 'simplify'`，确认其 `allowedAgents` 字段：

```typescript
// 原值
allowedAgents: ['oracle'],
```

- [ ] **Step 2: 将 oracle 改为 fixer**

```typescript
// 改为
allowedAgents: ['fixer'],
```

原因: orchard 在 v2.0.0 中为只读代理（Tier-3），无法执行 `write` 操作，而 simplify 技能需要落地代码变更。fixer 才是正确的执行者。

- [ ] **Step 3: 编译验证**

```bash
bun run build
```

- [ ] **Step 4: 提交**

```bash
git add src/cli/custom-skills.ts
git commit -m "fix: reassign simplify skill from oracle to fixer (oracle is read-only in v2)"
```

### Task A5：修改 `src/config/agent-mcps.ts` —— MCP 名更新 + 注入门控

**Files:**
- Modify: `omo-slim-v2-target\src\config\agent-mcps.ts`

- [ ] **Step 1: 全局替换 MCP 名**

```typescript
// 查找所有 'grep_app'，替换为 'gh_grep'
// 可能涉及的位置：
// - allBuiltinMcps 对象键
// - 字符串引用
// - 测试文件中的引用
```

精确替换（使用编辑工具逐处替换）:

```
oldString: 'grep_app'
newString: 'gh_grep'
replaceAll: true
```

- [ ] **Step 2: 在文件顶部添加导入**

```typescript
import { getRestrictedMcpDenies } from './agent-mcp-blacklist';
```

- [ ] **Step 3: 在 `buildAgentMcpPermissionRules` 函数末尾追加黑名单拒绝规则**

在 `return permissions;` 之前添加：

```typescript
// --- patch-kit v2: restricted MCP deny rules ---
const restrictedDenies = getRestrictedMcpDenies(agentName);
Object.assign(permissions, restrictedDenies);
// --- end patch-kit v2 ---
```

- [ ] **Step 4: 编译验证**

```bash
bun run build
```

- [ ] **Step 5: 提交**

```bash
git add src/config/agent-mcps.ts
git commit -m "fix: rename grep_app to gh_grep; inject restricted MCP deny rules"
```

### Task A6：修改 Agent Factory 文件 —— 追加 cancel_task deny + permission 块

**Files:**
- Modify: `omo-slim-v2-target\src\agents\oracle.ts`
- Modify: `omo-slim-v2-target\src\agents\explorer.ts`
- Modify: `omo-slim-v2-target\src\agents\librarian.ts`
- Modify: `omo-slim-v2-target\src\agents\observer.ts`
- Modify: `omo-slim-v2-target\src\agents\council.ts`
- Modify: `omo-slim-v2-target\src\agents\fixer.ts`
- Modify: `omo-slim-v2-target\src\agents\designer.ts`

**说明：** v2.0.0 agent factories 与 v1.1.1 完全一致。需要给已有 permission deny 块追加 `cancel_task: 'deny'`。对于 v2.0.0 中尚未有 permission 块的 agent，需要新增。

- [ ] **Step 1: 修改只读代理（oracle, explorer, librarian, observer）**

这四个代理在 v2.0.0 中应该已有 permission deny 块。找到每个文件的 `permission:` 对象，在 deny 列表中添加一行 `cancel_task: 'deny'`。

以 oracle.ts 为例：

```typescript
// 找到 permission 对象，追加 cancel_task
permission: {
  edit: 'deny',
  write: 'deny',
  bash: 'deny',
  task: 'deny',
  todowrite: 'deny',
  cancel_task: 'deny',  // <-- 新增
},
```

对 explorer.ts, librarian.ts, observer.ts 同样操作。

- [ ] **Step 2: 修改 council**

```typescript
permission: {
  edit: 'deny',
  write: 'deny',
  bash: 'deny',
  todowrite: 'deny',
  cancel_task: 'deny',  // <-- 新增
},
```

council 保留 `task` allow（用于 councillor 分发），但拒绝 cancel_task。

- [ ] **Step 3: 修改 fixer, designer**

这两个代理的 permission 块只拒绝 `task`。追加 `cancel_task: 'deny'`：

```typescript
permission: {
  task: 'deny',
  cancel_task: 'deny',  // <-- 新增
},
```

- [ ] **Step 4: 编译验证**

```bash
bun run build
```

- [ ] **Step 5: 运行 agent factory 相关测试**

```bash
bun test -- --testPathPattern="agents/"
```

- [ ] **Step 6: 提交**

```bash
git add src/agents/oracle.ts src/agents/explorer.ts src/agents/librarian.ts src/agents/observer.ts src/agents/council.ts src/agents/fixer.ts src/agents/designer.ts
git commit -m "feat: add cancel_task deny to all non-orchestrator agent factories"
```

### Task A7：修改 `src/agents/index.ts` —— orchestrator 前缀匹配 + 移除 shadow agent

**Files:**
- Modify: `omo-slim-v2-target\src\agents\index.ts`

- [ ] **Step 1: 添加导入**

```typescript
import { isOrchestratorAgent } from '../utils/orchestrator-identity';
import { getRestrictedMcpDenies } from '../config/agent-mcp-blacklist';
```

- [ ] **Step 2: 泛化 orchestrator 身份检查**

找到 `applyClassification` 函数中判断 mode='primary' 的位置。v1.1.1 中是 `agentName === 'orchestrator'`，v2.0.0 结构可能相同。

```typescript
// 将
if (agentName === 'orchestrator') {
  classification.mode = 'primary';
}

// 改为
if (isOrchestratorAgent(agentName)) {
  classification.mode = 'primary';
}
```

- [ ] **Step 3: 注入 MCP 黑名单到 agent config**

在 `getAgentConfigs` 或等价的 agent config 构建函数中，找到 permission 构建点，追加：

```typescript
// 在 permission 构建循环中追加
const restrictedDenies = getRestrictedMcpDenies(agentName);
Object.assign(agentConfig.permission, restrictedDenies);
```

- [ ] **Step 4: 移除 shadow agent 注册（如果 v2.0.0 中存在）**

搜索 `TASK_FALLBACK_AGENT_SUFFIX`、`getTaskFallbackShadowName`、`getTaskFallbackShadowConfig`。如果这些符号存在于文件中，删除以下内容：

```typescript
// 删除这些定义（如存在）
const TASK_FALLBACK_AGENT_SUFFIX = '__task_fallback';
function getTaskFallbackShadowName(agentName: string): string { ... }
function getTaskFallbackShadowConfig(agentName: string, ...): AgentConfig | undefined { ... }

// 删除 shadow agent 注册循环（如存在）
for (const [name, agent] of Object.entries(agents)) {
  const shadowName = getTaskFallbackShadowName(name);
  const shadowConfig = getTaskFallbackShadowConfig(name, agent);
  if (shadowConfig) {
    agents[shadowName] = shadowConfig;
  }
}
```

原因: v2.0.0 的 OpenCode 原生 background subagent 自行处理模型回退，shadow agent 机制不再需要。

- [ ] **Step 5: 编译验证**

```bash
bun run build
```

- [ ] **Step 6: 提交**

```bash
git add src/agents/index.ts
git commit -m "feat: generalize orchestrator check; inject MCP blacklist; remove shadow agent registration"
```

### Task A8：修改 `src/index.ts` —— 入口点集成

**Files:**
- Modify: `omo-slim-v2-target\src\index.ts`

**说明：** v2.0.0 的 `src/index.ts` 已大幅重构（Divoom→Companion, SessionManager→BackgroundJobBoard）。需要针对 v2.0.0 的实际源码定位以下 3 个集成点。**本任务需要阅读 v2.0.0 的实际 src/index.ts 才能给出精确行号。**

- [ ] **Step 1: 阅读 v2.0.0 的 `src/index.ts`，定位以下关键位置**

打开文件，搜索以下关键符号确定行号：

| 集成点 | 搜索关键词 | 用途 |
|--------|-----------|------|
| 配置合并点 | `config.agents` 或 `getAgentConfigs` | 注入 `mergeAgentConfig()` |
| 冷却存储初始化 | `ForegroundFallbackManager` 或 `foregroundFallback` | 传递 cooldown store |
| 启动模型选择 | `effectiveArrays` 或 model selection 循环 | 冷却感知模型跳过 |

- [ ] **Step 2: 集成点 1 —— 配置合并（mergeAgentConfig）**

在 agent config 构建完成后（通常在 `config()` hook 中），找到类似以下模式的代码：

```typescript
// 找到将 agent config 合并到最终输出的位置
for (const [name, agent] of Object.entries(agents)) {
  // ...
}
```

在循环体内追加：

```typescript
import { mergeAgentConfig } from './utils/merge-agent-config';

// 在 agent 配置合并点：
agentConfig = mergeAgentConfig(pluginDefaultAgent, agentConfig);
```

- [ ] **Step 3: 集成点 2 —— ForegroundFallbackManager 冷却存储**

找到 `new ForegroundFallbackManager(` 或等价构造函数调用。v2.0.0 中该类的构造函数签名可能与 v1.1.1 不同。

v1.1.1 补丁后的签名为：
```typescript
constructor(
  ctx, chains, config,
  cooldowns?: CooldownStore,
  onSessionAgentChange?: (sessionID: string, agent: string) => void
)
```

在调用处传入冷却存储：
```typescript
import { createCooldownStore, getDefaultCooldownPath } from './hooks/foreground-fallback/cooldowns';

const cooldowns = createCooldownStore({ filePath: getDefaultCooldownPath() });

const foregroundFallback = new ForegroundFallbackManager(
  ctx, chains, config,
  cooldowns,  // <-- 新增参数
  (sessionID, agent) => { sessionAgentMap.set(sessionID, agent); }  // <-- 新增回调
);
```

**如果 v2.0.0 的 ForegroundFallbackManager 签名不同，需要先阅读其构造函数定义再适配。**

- [ ] **Step 4: 集成点 3 —— 启动模型选择冷却感知**

此集成点取决于 v2.0.0 的实际代码结构。在 v1.1.1 中，`effectiveArrays` 循环在 `config()` hook 中遍历代理链，选择第一个非冷却模型。

在 v2.0.0 中找到等效的启动模型选择逻辑。如果是类似循环模式：

```typescript
// v1.1.1 补丁后的模式（参考）
for (const [agentName, chain] of Object.entries(effectiveArrays)) {
  const model = chain.find(m => !cooldowns.isCoolingDown(m));
  // ...
}
```

将此冷却感知逻辑注入 v2.0.0 的等效位置。

- [ ] **Step 5: 编译验证**

```bash
bun run build
```

- [ ] **Step 6: 提交**

```bash
git add src/index.ts
git commit -m "feat: integrate mergeAgentConfig, cooldown store, and cooldown-aware model selection"
```

### Task A9：修改 `src/hooks/foreground-fallback/index.ts` —— 冷却追踪集成

**Files:**
- Modify: `omo-slim-v2-target\src\hooks\foreground-fallback\index.ts`

- [ ] **Step 1: 确认 v2.0.0 的 ForegroundFallbackManager 构造函数签名**

打开文件，记录构造函数参数列表。

- [ ] **Step 2: 添加 cooldowns 参数支持**

如果构造函数尚未包含 cooldowns 参数，追加：

```typescript
import type { CooldownStore } from './cooldowns';

constructor(
  // ... 现有参数 ...
  private readonly cooldowns?: CooldownStore,
  private readonly onSessionAgentChange?: (sessionID: string, agent: string) => void,
) {
```

- [ ] **Step 3: 在限流错误处理中调用 captureCooldown**

找到 `tryFallback` 或等效的限流处理函数，在检测到 rate-limit error 时追加：

```typescript
// 在 rate-limit error 处理分支中：
if (error?.data?.responseHeaders && this.cooldowns) {
  this.captureCooldown(sessionID, error);
}
```

- [ ] **Step 4: 在链选择中注入冷却感知过滤**

找到 fallback 模型链遍历逻辑，追加冷却过滤：

```typescript
private tryFallback(sessionID: string, ...) {
  const chain = this.chains.get(agentName);
  // 优先选择非冷却模型
  const preferred = chain.filter(m => !this.cooldowns?.isCoolingDown(m));
  const candidate = preferred.length > 0 ? preferred : chain;
  // ... 继续现有逻辑
}
```

- [ ] **Step 5: 添加 `captureCooldown` 私有方法**

```typescript
import { parseAnthropicCooldown } from './cooldowns';

private captureCooldown(sessionID: string, error: any): void {
  if (!this.cooldowns) return;
  const headers = error?.data?.responseHeaders;
  if (!headers) return;
  const resetEpoch = parseAnthropicCooldown(headers);
  if (resetEpoch === null) return;
  const model = this.getCurrentModel(sessionID); // 或等价方法获取当前模型
  if (model) {
    this.cooldowns.set(model, resetEpoch);
  }
}
```

- [ ] **Step 6: 添加 `getCooldownStore` 公开方法**

```typescript
getCooldownStore(): CooldownStore | undefined {
  return this.cooldowns;
}
```

- [ ] **Step 7: 编译验证**

```bash
bun run build
```

- [ ] **Step 8: 运行 fallback 相关测试**

```bash
bun test -- --testPathPattern="foreground-fallback/"
```

- [ ] **Step 9: 提交**

```bash
git add src/hooks/foreground-fallback/index.ts
git commit -m "feat: integrate cooldown tracking into ForegroundFallbackManager"
```

### Task A10：修改 `package.json` —— 构建脚本

**Files:**
- Modify: `omo-slim-v2-target\package.json`

- [ ] **Step 1: 添加 clean:dist 脚本**

```json
"scripts": {
  "clean:dist": "bun run scripts/clean-dist.ts",
  // ... 保留 v2.0.0 原有 scripts
}
```

- [ ] **Step 2: 在 build 脚本前追加 clean:dist**

将 `"build"` 脚本修改为先清理后构建：

```json
"build": "bun run clean:dist && <原有 build 命令>"
```

例如，如果 v2.0.0 原 build 为 `"build:plugin && build:cli && tsc && generate-schema"`：

```json
"build": "bun run clean:dist && build:plugin && build:cli && tsc && generate-schema"
```

- [ ] **Step 3: 提交**

```bash
git add package.json
git commit -m "build: add clean:dist step before build"
```

### Task A11：全量测试 —— 确认 Phase A 完整性

- [ ] **Step 1: 清理构建**

```bash
bun run clean:dist
```

- [ ] **Step 2: 完整构建**

```bash
bun run build
```

预期: 构建成功，无 TypeScript 错误。

- [ ] **Step 3: 运行全部测试**

```bash
bun test
```

记录测试结果。如果有个别测试失败，分析是否与 Phase A 修改相关。

- [ ] **Step 4: 与基线对比**

将测试结果与 Task A1 Step 3 的基线对比。预期：
- 无新增失败（由 Phase A 修改引起的回归）
- 新增的 14 个测试文件（7 个模块 × 2）的测试应该通过

- [ ] **Step 5: 提交**

```bash
git add -A
git commit -m "chore: Phase A complete - all tests passing"
```

---

## Phase B：异步编排语义（目标：background orchestration 可用）

### Task B1：创建 `background-orchestration` companion skill

**Files:**
- Create: `E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\skills\background-orchestration\SKILL.md`

- [ ] **Step 1: 创建技能目录**

```powershell
New-Item -ItemType Directory -Path "E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\skills\background-orchestration" -Force
```

- [ ] **Step 2: 编写 SKILL.md**

```markdown
---
name: background-orchestration
description: Use when orchestrating superpowers workflows on platforms with background (non-blocking) subagent dispatch. Teaches the orchestrator how to dispatch, wait for results, and proceed through phases in an async model.
---

# Background Orchestration for Superpowers Workflows

## When to Use

Use this skill when:
- The platform supports background/non-blocking subagent dispatch (e.g., OpenCode `task(background=true)`)
- You need to execute superpowers workflows (brainstorm → plan → implement → review → verify → finish) on such a platform
- You are the **orchestrator** agent

## Core Principle

Superpowers phases have dependencies. `implement` must wait for `plan`. `review` must wait for `implement`. In a blocking model, the tool handled this for you. In a background model, **you** must enforce it explicitly.

## The Async Dispatch Pattern

### Phase 1: Dispatch

When entering a phase that requires subagent work (implement, review, parallel exploration):

1. Construct a **self-contained task packet** for each subagent:
   - Goal and acceptance criteria
   - Relevant plan/spec excerpts
   - File paths and constraints
   - Any prior phase outputs needed

2. Dispatch ALL independent tasks for this phase using the platform's background task tool:
   ```
   task(description="...", prompt="...", subagent_type="fixer", background=true)
   ```

3. Record each dispatched task: its ID, agent type, purpose, and which phase it belongs to.

4. **End your turn.** Do NOT poll. Do NOT loop. The system will reactivate you when results arrive.

### Phase 2: Collect

When your session receives synthetic completion messages (the platform injects them automatically):

1. Identify which phase each completion belongs to (from your records in Phase 1).
2. Check if ALL tasks for the current phase have completed.
3. If some are still running, wait for more completions (end turn).
4. If ALL tasks for the phase are done → proceed to Phase 3.

### Phase 3: Gate Check

Before advancing to the next superpowers phase:

1. **Implement → Review gate**: All implement tasks completed with DONE status → dispatch review.
2. **Review → Verify gate**: All review tasks returned approval → run verification.
3. **Verify → Finish gate**: Verification commands produce expected output → declare completion.

If a gate fails:
- For implement failures: re-dispatch the failed task with corrected context.
- For review rejections: re-dispatch implement with reviewer feedback.
- For verification failures: fix the issue and re-verify.

## Parallelism Rules

| Can be parallel | Must be sequential |
|----------------|-------------------|
| Multiple independent implement tasks | implement before review |
| Multiple @explorer recon tasks | plan before implement |
| Multiple best-of-N candidate generators | review before verify |
| Multiple test files for different subsystems | verify before finish |

## Task Packet Template

When dispatching a background subagent, include:

```
Goal: [one-sentence objective]
Plan Context: [relevant plan section or summary]
Files: [list of files to work with, with paths]
Constraints: [coding standards, patterns to follow]
Prior Outputs: [results from previous phases needed]
Acceptance: [what "done" looks like]
```

The subagent must receive EVERYTHING it needs. There is no `read_session` to fetch missing context later.

## Phase State Tracking

Maintain a simple state record (mental or in a scratch file):

```
Phase: implement
Tasks:
  - job-abc: @fixer → user-service.ts → [RUNNING]
  - job-def: @fixer → auth-service.ts → [DONE]
Gate: waiting for job-abc
Next: review
```

Update this record each turn. It prevents you from losing track across multiple async cycles.

## Completion Declaration

Only declare the superpowers workflow COMPLETE after:
1. All phases have passed their gates
2. The verification phase produced passing evidence
3. You have fresh verification output (not cached from a previous session)
```

- [ ] **Step 3: 验证技能文件格式**

确认 frontmatter 包含 `name` 和 `description` 字段。确认内容无 emoji（RobCo UOS 终端不支持）。

- [ ] **Step 4: 提交**

```bash
git add skills/background-orchestration/SKILL.md
git commit -m "feat: add background-orchestration companion skill for async superpowers workflows"
```

### Task B2：扩展 prompt bridge —— orchestrator 异步调度指令

**Files:**
- Modify: `E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\prompt-bridges\orchestrator_append.md`

- [ ] **Step 1: 阅读现有 orchestrator_append.md**

确认当前内容不包含 `subtask` 引用（阶段0验证已确认）。

- [ ] **Step 2: 追加异步调度段落**

在文件末尾追加：

```markdown

## Background Orchestration Awareness

When dispatching subagents on a platform that supports background/non-blocking execution:

- After dispatching all tasks for the current phase, **end your turn**. Do not poll or loop.
- The system will inject synthetic completion messages when background tasks finish.
- When reactivated, first check which tasks completed, then proceed to the next phase gate.
- Maintain a mental phase state to track which tasks are pending/running/done across async cycles.
- Use `superpowers:background-orchestration` skill for detailed async orchestration rules.

Prefer `task(..., background: true)` for work that can run independently. Use foreground dispatch only for quick, immediate-return operations.
```

- [ ] **Step 3: 提交**

```bash
git add prompt-bridges/orchestrator_append.md
git commit -m "feat: add background orchestration awareness to orchestrator bridge"
```

### Task B3：扩展 prompt bridge —— worker 代理自验证指令

**Files:**
- Modify: `E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\prompt-bridges\fixer_append.md`
- Modify: `E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\prompt-bridges\designer_append.md`

- [ ] **Step 1: 在 fixer_append.md 末尾追加**

```markdown

## Background Task Self-Verification

Since you may be running as a background subagent with no parent-session context access:

1. Your task prompt contains ALL context you need. Do not expect to read parent session history.
2. Before returning DONE, run verification commands (tests, type checks, lints) and include their output.
3. If you cannot complete the task with the provided context, return BLOCKED with specific missing information.
```

- [ ] **Step 2: 在 designer_append.md 末尾追加相同内容**

- [ ] **Step 3: 提交**

```bash
git add prompt-bridges/fixer_append.md prompt-bridges/designer_append.md
git commit -m "feat: add background-task self-verification instructions to worker bridges"
```

### Task B4：扩展 `verification-before-completion` 语义（轻量扩展）

**说明：** 不修改 upstream superpowers 的 `verification-before-completion/SKILL.md`。改为在 bridge 中追加 persistent verification 条款。

**Files:**
- Modify: `E:\云文件\GitHub\Meow-omo-slim-superpowers-patch-kit\prompt-bridges\orchestrator_append.md`

- [ ] **Step 1: 在 orchestrator bridge 的 verification 相关段落追加**

```markdown

## Async Verification Rule

In background orchestration mode, verification evidence must be FRESH — produced in the current verification phase, not carried over from a worker's self-verification in a prior phase. After collecting all worker results, run at least one verification command (test suite, type check, lint) directly before declaring completion. Worker self-verification is supplementary, not sufficient.
```

- [ ] **Step 2: 提交**

```bash
git add prompt-bridges/orchestrator_append.md
git commit -m "feat: add async verification rule to orchestrator bridge"
```

---

## Phase C：端到端验证

### Task C1：构建最终产物

- [ ] **Step 1: 完整清理构建**

```bash
bun run clean:dist
bun run build
```

预期: 构建成功。

- [ ] **Step 2: 运行全部测试**

```bash
bun test
```

预期: 全部通过（包括新增的 14 个策略模块测试 + cooldown 测试 + 现有测试无回归）。

- [ ] **Step 3: 生成补丁文件**

```bash
git diff v2.0.0 --patch > ../patches/oh-my-opencode-slim/0000-combined-v2.0.0.patch
```

- [ ] **Step 4: 提交**

```bash
git add -A
git commit -m "release: patch-kit v2.0.0 for OMO Slim v2.0.0"
```

### Task C2：端到端工作流验证（冒烟测试）

**说明：** 此步骤需要在安装了补丁后 OMO Slim 的 OpenCode 实例上进行。

- [ ] **Step 1: 启动 OpenCode with background subagents enabled**

```bash
OPENCODE_EXPERIMENTAL_BACKGROUND_SUBAGENTS=true opencode
```

- [ ] **Step 2: 验证 orchestrator 身份**

在 OpenCode 中选择 `orchestrator` 代理。输入：

```
load superpowers:using-superpowers
load superpowers:background-orchestration
```

确认两个技能均加载成功。

- [ ] **Step 3: 验证 superpowers 技能门控**

依次切换代理并尝试加载技能：

| 代理 | 尝试加载 | 预期结果 |
|------|---------|---------|
| fixer | `brainstorming` | 拒绝（不在白名单） |
| fixer | `test-driven-development` | 允许 |
| oracle | `systematic-debugging` | 允许 |
| oracle | `brainstorming` | 拒绝 |
| explorer | 任何 superpowers 技能 | 全部拒绝 |

- [ ] **Step 4: 验证 MCP 门控**

检查 `grep_app` 已更名为 `gh_grep`，门控规则仍生效。

- [ ] **Step 5: 验证 background task 调度**

```text
# 派发一个非阻塞子任务
task(description="test background dispatch", prompt="echo hello world and return DONE", subagent_type="fixer", background=true)
```

确认：
- 命令立即返回（不阻塞）
- 稍后在会话中收到 synthetic completion message

- [ ] **Step 6: 验证 cancel_task 拒绝**

切换到 fixer 代理，尝试调用 cancel_task。确认被拒绝（非 orchestrator）。

- [ ] **Step 7: 验证冷却追踪持久化**

检查 `~/.config/opencode/.omo-slim-cooldowns.json` 文件存在（首次限流后生成）。

---

## 风险与回滚

### 风险

| 风险 | 应对 |
|------|------|
| v2.0.0 `src/index.ts` 结构未知，集成点需要实地定位 | Task A8 设计为"阅读后编辑"，而非预设补丁 |
| ForegroundFallbackManager 构造函数签名可能变化 | Task A9 同样先阅读后适配 |
| OpenCode background subagent 仍为实验性功能 | 锁定已知可用的 OpenCode 版本 |
| task_status 工具版本依赖 | 阶段0验证时确认目标版本的工具可用性 |
| 新增测试文件可能需要调整导入路径 | 编译步骤会立即暴露，逐个修复 |

### 回滚

撤销 `v2.0.0...patch-kit-v2` 分支的所有提交，恢复纯净 v2.0.0：

```bash
git checkout v2.0.0
git branch -D patch-kit-v2
```

或仅回滚到 Phase A 完成状态（保留访问控制层但不启动异步编排）：

```bash
git revert <Phase B 的提交范围>
```

---

## 产出物清单

实施完成后，以下文件将被创建或修改：

### 新增文件（在 OMO Slim v2.0.0 源码中）
1. `src/cli/superpowers-policy.ts`
2. `src/cli/superpowers-policy.test.ts`
3. `src/cli/agent-tier-policy.ts`
4. `src/cli/agent-tier-policy.test.ts`
5. `src/config/agent-mcp-blacklist.ts`
6. `src/config/agent-mcp-blacklist.test.ts`
7. `src/config/orchestrator-only-skills.ts`
8. `src/config/orchestrator-only-skills.test.ts`
9. `src/utils/orchestrator-identity.ts`
10. `src/utils/orchestrator-identity.test.ts`
11. `src/utils/merge-agent-config.ts`
12. `src/utils/merge-agent-config.test.ts`
13. `src/hooks/foreground-fallback/cooldowns.ts`
14. `src/hooks/foreground-fallback/cooldowns.test.ts`
15. `scripts/clean-dist.ts`

### 新增文件（在 patch-kit 仓库中）
16. `skills/background-orchestration/SKILL.md`

### 修改的 OMO Slim 源文件
17. `src/cli/skills.ts`
18. `src/cli/custom-skills.ts`
19. `src/config/agent-mcps.ts`
20. `src/agents/index.ts`
21. `src/index.ts`
22. `src/hooks/foreground-fallback/index.ts`
23. `package.json`
24. `src/agents/oracle.ts`
25. `src/agents/explorer.ts`
26. `src/agents/librarian.ts`
27. `src/agents/observer.ts`
28. `src/agents/council.ts`
29. `src/agents/fixer.ts`
30. `src/agents/designer.ts`

### 修改的 patch-kit 文件
31. `prompt-bridges/orchestrator_append.md`
32. `prompt-bridges/fixer_append.md`
33. `prompt-bridges/designer_append.md`

### 移除的补丁
- 0007-final-orchestrator-pivot-cleanup.patch（全部内容）
- 0004-orchestrator-prefix-matching.patch 的 shadow agent 注册部分

---

*Plan saved to: `docs/superpowers/plans/2026-06-14-patch-kit-v2.0.0-implementation.md`*
*Estimated total: 1-1.5 weeks (one engineer)*
*Remember: A better future, underground!*
